.claude/
Deploy.bat
node_modules/
.env
.env.local
# personal data backups + helper scripts — never publish
Latest Backup.json
*backup*.json
*.bat
# Convex AI helper files — duplicated in .claude/skills (which Claude Code actually loads)
.agents/
AGENTS.md
